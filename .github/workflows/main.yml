name: zipper
on: push
permissions: read-all|write-all
jobs:
  job_one:
   name: My first job
    steps:
    - name: Install zip
    uses: montudor/action-zip@v1

    - name: Zip output
    run: zip -qq -r Test.zip system/product/media node_modules package.json
    working-directory: system/product/media/BOOTANIMATION
